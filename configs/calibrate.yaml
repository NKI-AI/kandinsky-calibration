# @package _global_

defaults:
  - _self_
  - data: coco
  - model: segmentation
  - callbacks: default_calibrate
  - logger: tensorboard
  - trainer: default
  - paths: default
  - extras: default
  - hydra: default
  - experiment: null

task_name: "calibrate"

tags: ["dev"]

# passing checkpoint path is necessary for evaluation
ckpt_path: ???
